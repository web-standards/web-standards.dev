{% from 'news-card.njk' import newsCard %}

<ul class="news-list">
{%- for item in pagination.items %}
	<li>
		{{ newsCard(news=item, fetchpriority='high' if loop.index <= 4 else '') }}
	</li>
{%- endfor %}
</ul>

{% include 'pagination.njk' %}
