The new `<usermedia>` element manages camera and microphone access, handling prompts and `MediaStream` delivery without a separate `getUserMedia()` call. The `shadowrootslotassignment` attribute on `<template>` enables manual slot assignment in HTML. Two new performance entries, `soft-navigation` and `interaction-contentful-paint`, measure interaction-driven timing in single-page apps.
