Josh Comeau dives into CSS `animation-timeline`, mapping keyframes over scroll distance instead of duration. He covers timing functions, animation ranges (`cover`, `contain`, `entry`, `exit`), range percentages, scroll progress timelines, and linked timelines that track one element while animating another, with `view-timeline` and `timeline-scope`. Around 85% browser support, with Firefox behind a flag.
