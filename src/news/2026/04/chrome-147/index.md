Element-scoped view transitions allow multiple concurrent transitions on arbitrary elements. CSS `contrast-color()` returns black or white for best contrast. The `border-shape` property enables non-rectangular borders like polygons and circles. Other additions include `Math.sumPrecise`, timeline named range scroll, `CSSPseudoElement` interface, local network access restrictions, and Rust-based XML parsing.
