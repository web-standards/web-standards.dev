CSS name-only container queries can match by `container-name` without a `container-type`. The `at-rule()` function in `@supports` tests CSS at-rule support. Video and audio elements gain `loading="lazy"` for deferred loading. The Prompt API exposes on-device AI language models. Other additions include Web Serial API on Android, Web Authentication Immediate UI mode, `text-decoration-skip-ink: all`, and `revert-rule`.
