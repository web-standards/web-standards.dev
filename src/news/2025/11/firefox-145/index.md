Adds support for the `text-autospace` CSS property, the `math` generic font family, and `<hr>` in `<select>` menus on Android. JavaScript gains `Atomics.waitAsync()` for non-blocking thread sync, and HTTP introduces Integrity-Policy headers for script integrity. Matroska container support, new WebDriver BiDi events, and experimental features like Anchor Positioning and Trusted Types.
